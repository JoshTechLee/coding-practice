//https://www.tutorialspoint.com/java/index.htm

//Primitive data types
byte bits = 8;
short bits = 16;
int bits = 32;
long bits = 64;
float bits = 32.0;
double bits = 64.0;
char data = 'a'
String data = 'alphabet'
boolean flag = true;

//Loops
int n = 8;
for (int i = 0; i < n; i++){
  // do something "n" times
}
while (i < n){
  // do something "n" times
  i++;
}

//

//Repeating character for Java
String.format("%0" + numberOfChars + "d", 0).replace("0", substituteString);

//Limit number of chracters
String.format("%" + numberOfSpaces + "s", "");

//BigInteger
BigInteger num = BigInteger.valueOf(1);
num = num.multiply(BigInteger.valueOf(2));  // must reassign like regular primitive number
String s = num.toString();
